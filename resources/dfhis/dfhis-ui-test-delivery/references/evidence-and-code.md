# Evidence and code analysis

Use evidence in this order: current acceptance wording, developer comments, matching source, read-only environment observations, then historical knowledge. Label conclusions `source-confirmed`, `comment-derived`, `environment-observed`, or `inferred`. Record the system, menu path, component, trigger, condition, early return, downstream action, adjacent flows, and required test data. Never claim source inspection when only a comment was read.
